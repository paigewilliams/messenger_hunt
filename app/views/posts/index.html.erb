<p id="notice"><%= notice %></p>

<h1>Inbox</h1>

<table>
  <thead>
    <tr>
      <th>From</th>
      <th>Title</th>
      <th>Read</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @inbox_messages.each do |post| %>
      <tr>
        <td><%= User.find(post.created_by.to_i).email %></td>
        <td><%= post.title %></td>
        <td><%= status = post.read == true ? 'read' : 'unread' %></td>
        <td><%= link_to 'Show', post, :read =>'true' %></td>
        <!-- <td><%= link_to 'Edit', edit_post_path(post) %></td> -->
        <!-- <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td> -->
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<h1>Sent</h1>

<table>
  <thead>
    <tr>
      <th>To</th>
      <th>Title</th>
      <th>Body</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @sent_messages.each do |post| %>
      <tr>
        <td><%= User.find(post.send_to.to_i).email %></td>
        <td><%= post.title %></td>
        <td><%= post.body %></td>
        <!-- <td><%= link_to 'Show', post %></td> -->
        <!-- <td><%= link_to 'Edit', edit_post_path(post) %></td> -->
        <td><%= link_to 'Destroy', post, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Post', new_post_path %>
